# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ActionExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ActionExtension

end

target 'adblock-test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for adblock-test
  pod 'SwiftyJSON'

  target 'adblock-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'adblock-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'ContentBlockerExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ContentBlockerExtension

end
